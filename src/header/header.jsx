import React from "react";
import "./header-style.css";

const Header = props => {
  return (
    <header>
      <a href="#" className="login-alignment">
        Login / Register
      </a>
    </header>
  );
};

export default Header;
