import React from "react";
import "./header-style.css";

const Header = props => {
  return (
    <header>
      <a href="#" className="loginButton">
        Login / Register
      </a>
    </header>
  );
};

export default Header;
